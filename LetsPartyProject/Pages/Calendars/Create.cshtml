@page
@model LetsPartyProject.Pages.Calendars.CreateModel

@{ ViewData["Title"] = "Create"; }

<section id="sectionTeams" class="container">
    <header class="container mb-4">
        <div id="headerIndexPages" class="row align-middle">
            <div class="col-9">
                <h1>Member Create</h1>
            </div>
            <hr />
        </div>
    </header>
    <div id="formPages" class="row p-4">
        <div class="col">
            <form method="post">
                <div class="form-row">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group col">
                        <label asp-for="Calendar.CalendarName" class="control-label"></label>
                        <input asp-for="Calendar.CalendarName" class="form-control" />
                        <span asp-validation-for="Calendar.CalendarName" class="text-danger"></span>
                    </div>
                    <div class="form-group col">
                        <label asp-for="Calendar.TeamId" class="control-label"></label>
                        <select asp-for="Calendar.TeamId" class="form-control" asp-items="ViewBag.TeamId">
                            <option value="">-- Select your Team --</option>
                        </select>
                    </div>
                </div>
                <div class="form-group pt-4">
                    <input type="submit" value="Create" class="btn btn-block btn-outline-success" />
                    <a class="btn btn-block btn-outline-info" asp-page="Index">Back to List</a>
                </div>
            </form>
        </div>
    </div>
</section>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
